[project]
name = "neuraflow-rc-car-bridge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "customtkinter>=5.2.2",
    "inputs>=0.5",
    "pyserial>=3.5",
    "websockets>=16.0",
]
